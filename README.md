# GLzDiscordBot

This is a small custom Discord Bot i am Creating

U will need:
  ```
  pip install -U discord.py 
  pip install -U python-dotenv
  ```
  
  .env File:
  ```
  Token='PASTE'
  owner='PASTE'
  prefix='PASTE'
  presence='PASTE'
```
